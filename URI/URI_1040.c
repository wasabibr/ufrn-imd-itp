#include <stdio.h>

main(){
	
	float n1, n2, n3, n4, med;
	scanf("%f %f %f %f", &n1, &n2, &n3, &n4);
	med=2*n1+3*n2+4*n3+n4;
	printf("Media: %.1f\n", med);
	if (med>=7)		
		printf("Aluno aprovado.\n");
	else
		if (med==)
	
	return 0;
}
