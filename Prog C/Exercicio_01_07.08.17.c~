#include <stdio.h>
#include <math.h>
 
int main() {
 
	float a, b, c, delta, x1, x2;
	printf("Digite os valores de a, b e c: ");
	scanf("%f %f %f", &a, &b, &c);
	delta = pow(b,2) -4 * a * c;
	x1 = (-b + strq(delta))/2*a;
	x2 = (-b - strq(delta))/2*a;
	printf("A equação %fx² + %fx + %f = 0, possui as seguintes raizes:\n x1 = %f e x2 = %f", &a, &b, &c, &x1, &x2);
	return 0; 

}
